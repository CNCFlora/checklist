{{> header}}
    <div class="col-md-8">
        <table class="table">
            <thead>
                <tr>
                    {{#docs}}
                    <th>{{family}}</th>
                    <th>{{strings.delete_specie}}</th>
                    {{/docs}}
                </tr>
            </thead>
            <tbody>
            {{#docs}}
                {{#species}}
                <tr>
                    <td class="species"><span>{{scientificNameWithoutAuthorship}}<span></td>
                    <td><a href="{{base}}/delete/specie/{{scientificNameWithoutAuthorship}}" class="glyphicon glyphicon-remove"></a></td>
                </tr>
                {{#synonyms}}
                <tr>
                    <td class="synonym">&nbsp;&nbsp;&nbsp;&nbsp;<span>{{scientificNameWithoutAuthorship}}</span> (synonym)</td>
                </tr>
                {{/synonyms}}
                {{/species}}
            {{/docs}}
            </tbody>
        </table>
    </div>

    <div class="col-md-4">
        <form action="{{base}}/insert/specie" method='POST' class='form'>
        <h3>Adicionar espécie da Flora do Brasil</h3>
        <fieldset>
            <div class='form-group'>
                <input id="specie" name="specie" type="text" placeholder="" rel='complete' class='form-control'/>
            </div>
            <button class="btn btn-success">Adicionar</button>
        </fieldset>
        </form>
        <form action="{{base}}/insert/new" method='POST' class='form'>
        <h3>Adicionar espécie sem Flora do Brasil</h3>
        <fieldset>
            <legend>Caso a espécie a inserir não exista na Flora do Brasil ou esteja errada na mesma.</legend>
            <div class='form-group'>
                <label for="family">Familia</label>
                <input name="family" type="text" placeholder="Acanthaceae" class='form-control'/>
            </div>
            <div class='form-group'>
                <label for="scientificNameWithoutAuthorship">Nome cientifico</label>
                <input name="scientificNameWithoutAuthorship" type="text" placeholder="Aphelandra longiflora" class='form-control'/>
            </div>
            <div class='form-group'>
                <label for="scientificNameAuthorship">Autor(a)</label>
                <input name="scientificNameAuthorship" type="text" placeholder="S.Profice" class='form-control'/>
            </div>
            <div class='form-group'>
                <label for="taxonomicStatus">Status</label>
                <select id="taxonomicStatus" name="taxonomicStatus">
                    <option value="accepted">Aceito</option>
                    <option value="synonym">Sinônimo</option>
                </select>
            </div>
            <div class='form-group'>
                <label for="acceptedNameUsage">Nome aceito com autor (se sinônimo)</label>
                <input id="acceptedNameUsage"  name="acceptedNameUsage" type="text" placeholder="Aphelandra longiflora S.Profice" class='form-control'/>
            </div>
            <button class="btn btn-success">Adicionar</button>
        </fieldset>
        </form>
    </div>

{{> footer}}
